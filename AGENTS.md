# Agent rules for this repo

- Commit messages: short description only (a single concise line). No body,
  no bullet points, no footer.
- Never add a `Co-Authored-By` trailer or any co-author hook to commits.
- No em dashes (`—`) anywhere in this repo: code, comments, docs, UI copy.
  Use a hyphen, comma, or a new sentence instead.
